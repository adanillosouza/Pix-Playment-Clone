<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.PixClone" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <item name="android:statusBarColor">@color/colorPrimaryVariant</item>
        <item name="android:navigationBarColor">@color/colorBackground</item>
        <item name="materialCardViewStyle">@style/Widget.Card</item>
    </style>

    <style name="Widget.Card" parent="Widget.MaterialComponents.CardView">
        <item name="cardUseCompatPadding">true</item>
        <item name="cardCornerRadius">@dimen/corner_lg</item>
        <item name="cardElevation">@dimen/elevation_sm</item>
    </style>
</resources>
